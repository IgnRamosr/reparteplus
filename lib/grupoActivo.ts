/*// lib/grupoActivo.ts
import AsyncStorage from '@react-native-async-storage/async-storage';

const KEY = 'grupoIdActivo';

export const setGrupoIdActivo = (id: string) => AsyncStorage.setItem(KEY, String(id));
export const getGrupoIdActivo = () => AsyncStorage.getItem(KEY);
export const clearGrupoIdActivo = () => AsyncStorage.removeItem(KEY);
/*/
