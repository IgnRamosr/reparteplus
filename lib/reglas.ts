/*// lib/reglas.ts
import { api } from '@/lib/api';

export async function puedeModificarParticipantes(grupoId: string): Promise<boolean> {
  // centralizamos aquí por si más adelante agregas caching o lógica adicional
return api.puedeModificarParticipantes(grupoId);
}/*/
